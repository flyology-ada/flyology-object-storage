# GetBucketVersioning qualification

`GetBucketVersioning` is reviewed through
`Flyology.Object_Storage.Client.Buckets.Get_Versioning`. The synchronous and
composable forms use the same signed request and bounded XML decoder. A 200
response with omitted `Status` and `MFADelete` is the successful
`Versioning_Unconfigured` state; it is not an absence result.

The focused socket evidence covers typed enabled and unconfigured responses,
structured authorization rejection, ordinary same-operation restart,
pre-admission cancellation, and admitted cancellation followed by complete
transport drain, typed `Finish`, retained-owner rejection, and same-operation
restart. Native and lightweight scheduling rounds exercise the same lifecycle.

The operation is read-only. Qualification does not select directory-bucket or
S3 Express endpoints, discover or rewrite access-point, Object Lambda, or
Outposts endpoints, follow cross-region redirects, or authorize a versioning
or MFA Delete mutation. Callers supply the exact origin and addressing style.

The maintained qualification lane runs the independent preparation verifier,
warning-strict test build, signed socket corpus, 116-operation coverage gate,
fresh selected public documentation, pinned-model repository gate, and final
diff check. A documentation warning or any missing subgate prevents a
qualification claim.
